# 整句评测剩余 miss 收尾记录（2026-09-18）

> **退役注记(2026-09-20)**:文中 `--neural data/model/model.qjm` 命令已失效(该模型与 crate 已移除),
> 现用 `--qwen <gguf>`;数字为当时口径,保留作历史对照。


> **2026-09-18 晚更新**：随包神经模型已替换为 [uer/gpt2-chinese-cluecorpussmall](https://huggingface.co/uer/gpt2-chinese-cluecorpussmall)（Apache-2.0，102M）转制版
>（词表重排 + 独立输出头适配，加载器支持可选 `head.weight`）。评测（含 IT 词库）**首选 87.1% / 字准确率 98.7%**，下表 22 条 miss 中 5 条翻案
>
> **⚠ 口径警示（对抗审查结论）**：87.1% 是对 (λ, cap, margin, 分歧机制) 联合调出的尖峰——λ 偏离 0.5 掉 10pp+，cap 12→8 掉 12pp。它以真实日志 −1.6pp 整句为代价，
> 且净损失 7 句里 5-6 句是「引擎不再复现用户当年将错就错的错字」（指标把纠错记为失分），真倒退仅 2 条（量哈、前后拼音，0.1% 流量，机制：cap 允许模型压过个人习语证据）。
> 三把尺建议：then_top1（用户当年视角）/ now_top1（引擎现状）/ miss 时耗差。行动项：个人证据保护闸（神经分差小于个人习语证据时不许翻案）、评测集扩容到 500+ 句混域后重调。

>（坐几路、编程、母亲河、索引优化相关、软件架构相关），新增 1 条反向（他感冒了 ← 她）；剩余 17 条仍全部为模型判别力上限，A 类结论不变但
> 「重训」的参照模型升级为 110M 级。本文其余数字为替换前小模型口径，保留作历史对照。

评测集：`data/eval/baseline-eval.txt`（139 句，gitignore，仅本机）。最终指标：

| 配置 | 首选命中 | 字准确率 |
| --- | --- | --- |
| 纯 bigram（缺省词库+IT 领域库） | 71.9% | 95.8% |
| + 神经重排（同上） | **84.2%** | **98.0%** |

真实日志回放（1168 词 / 377 整句，快照对照基线 88.4 / 86.7）：88.3 / 86.7，零回归；带神经重排 89.0 / 90.2。

本轮（对抗判定 → 修复 → 复测）共消掉 9 条 miss：索引优化、重新编译、软件架构、你的邮箱、一瞬间、花都开了、三份发给(部分)、微信句(稳态)、母亲河(部分)。
推理侧确定性杠杆已穷举留档：λ（0.4–4，全局提是负收益）、cap（8→12）、重排门槛（3/4/5/6/8/12/20）、束宽、格子宽度（6 最优）、词插入罚（各值均负收益）、typo 四类代价全组合（全量上调真实回放倒退，只采信 correction_penalty 5→6）。

## 剩余 22 条 miss 的根因结论（以本记录收尾，不再作为未完成项）

修复前提是外部资源到位，本机不具备：**训练仓库 `../train` 不存在**（青简目录下只有 qingjian），**语料 `data/corpus/` 为空**。逐条定责由对抗判定智能体实测（词库 grep、单句查询、λ/typo 运行时实验）交叉证实。

### A 类：需重训字级神经模型（17 条，训练仓库缺）

| # | miss（期望 ← 当前） | 根因证据 |
| --- | --- | --- |
| 1 | 需要再研究一下 ← 需要**在**研究 | 再/在，神经模型方向不够（λ≥2 才翻，全局提 λ 净负） |
| 2 | 应该**坐**几路 ← 做 | 坐 58984 < 做 285963，静态输；神经 Δ=4.0 不足 |
| 3 | **她**的笑容 ← 他 | 代词，无上下文不可判 |
| 6 | 发展**得**非常 ← 的 | 的/得，神经偏好不足 |
| 7 | 运行**得**很流畅 ← 的 | 神经实测偏好错（Δ=−0.4） |
| 8 | 数据分析**师**需要 ← 是 | 师/是，单测即错 |
| 9 | 学好**编程** ← 变成 | 神经实测偏好错（变成 −5.7 nat）；编程 1354 vs 变成 27280 |
| 10 | 说**得**非常 ← 的 | 的/得 |
| 11 | 老师夸奖**他** ← 她 | 代词 |
| 13 | 不要**再**犹豫 ← 在 | 同 #1，且神经此处方向反 |
| 15 | **她**是一名 ← 他 | 代词 |
| 16 | 快乐**地**奔跑 ← 的 | 地/的 |
| 18 | 直播**带货** ← 大火 | 带货已补频 8000 仍输（大火走 typo 边 + 神经不足） |
| 19 | 他的**画** ← 话 | 话 130374 > 画 40757，神经不翻 |
| 20 | 银杏**叶** ← 也 | 银杏叶已补频 8000 仍输精确拼音的 银杏+也 |
| 21 | 江边**乘凉** ← 将变成两 | 乘凉已补频 2000，池内第 4，神经不足 |
| 22 | 不曾**起舞** ← 器物 | 起舞 211 在库，同音竞争 + 神经不足 |

### B 类：需语料管线重统计（5 条，data/corpus 缺）

| # | miss | 根因证据 |
| --- | --- | --- |
| 4 | 打印**三份**发给 ← 三分 | 三份已补 5000 仍低于 三分；再调频会坏篮球语境，需语料定频 |
| 5 | **会议室**已被预定 ← 会议时 | 会议室已补 5000 仍输 会议+时 的 bigram，需语料重统计 |
| 12 | 困难**吓倒** ← 吓到 | 吓倒已补 3000 < 吓到 4818，两词皆真，需语料分频 |
| 14 | 中华民族的**母亲河** ← 母亲和 | 母亲河已补 5000 仍输 的+和，需语料/LM |
| 17 | 微风**轻拂** ← 轻服 | 轻拂已补 5000；字级 拂 在 fu 格第 7（截断线外），需语料重统计 |

## 复开条件与步骤

`../train` 与 `data/corpus` 就位后：

1. 重训字级模型：按训练仓库流程导出 `model.safetensors`，`dict-convert pack model` 出新 `.qjm`；重训语料保留 「的/得/地」「再/在」「她/他」 最小对照对，重训后在 eval 集回归。
2. 语料重统计：`dict-convert bigram data/corpus/*.txt` 重出 lm.qj；`mine` 重挖 oov（三份/会议室/吓倒/轻拂/银杏叶 的频次让语料说话）。
3. 验收命令：

   ```bash
   cargo run --release -p qingjian-cli -- --eval-text data/eval/baseline-eval.txt \
     --extra-dict data/generated/dicts/idioms.qj --extra-dict data/generated/dicts/it_computing.qj \
     --neural data/model/model.qjm
   cargo run --release -p qingjian-cli -- --replay <input-log> --neural data/model/model.qjm
   ```

   目标：A 类 17 条首选翻正、B 类 5 条翻正，回放（词/整句）不低于 88.3 / 86.7。
